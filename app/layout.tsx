import type { Metadata } from 'next';
import { Poppins } from 'next/font/google';
import './globals.css';

const poppins = Poppins({
  subsets: ['latin'],
  weight: ['100', '200', '300', '400', '500', '600', '700', '800', '900'],
  variable: '--font-poppins',
  display: 'swap',
});

export const metadata: Metadata = {
  title: 'Imalsha Portfolio | UI/UX Designer | Sri Lanka |',
  description:
    "Explore Imalsha's portfolio: a showcase of stunning, innovative designs blending creativity and functionality. Discover projects, skills, and expertise in [specific design fields, e.g., graphic design, UI/UX, branding, etc.]. Let's create something extraordinary together!",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${poppins.variable} antialiased`}>{children}</body>
    </html>
  );
}
